# ============================================================
# PostgreSQL Production Stack — Server Config Template
# Copy to .env and fill real values before first run.
# Never commit .env to version control.
# ============================================================

# ============================================================
# PostgreSQL Admin (Superuser)
# ============================================================
POSTGRES_USER=pgadmin
POSTGRES_PASSWORD=CHANGE_ME_STRONG_ADMIN_PASSWORD
# System database used by psql for admin connections and backup introspection.
# Keep as "postgres" unless you have a specific reason to change it.
POSTGRES_DB=postgres
POSTGRES_MAINTENANCE_DB=postgres

# ============================================================
# PgBouncer
# ============================================================
PGBOUNCER_LISTEN_PORT=6432

# ============================================================
# Backup
# ============================================================
BACKUP_DEST_PATH=/backups
BACKUP_RETENTION_DAYS=7

# ============================================================
# Monitoring
# ============================================================
POSTGRES_EXPORTER_PORT=9187

# ============================================================
# Network
# ============================================================
NETWORK_NAME=pg_private_net

# ============================================================
# Server public IP — used to bind PgBouncer port.
# Change to your VPN IP or 0.0.0.0 if firewall-protected.
# ============================================================
SERVER_PUBLIC_IP=91.239.232.91

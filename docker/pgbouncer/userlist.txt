# PgBouncer userlist.txt
# Формат: "username" "password"
# Паролі повинні збігатися з тими, що задані в PostgreSQL.
# SCRAM-SHA-256: замінити на фактичний хеш з pg_authid.rolpassword після init.
# Для початкового деплою можна використовувати plaintext (оновити після запуску).
#
# Щоб отримати SCRAM-хеш після запуску:
#   docker exec -it postgres psql -U pgadmin -c "SELECT rolname, rolpassword FROM pg_authid WHERE rolname='app1_user';"
# і скопіювати значення сюди у вигляді: "app1_user" "SCRAM-SHA-256$..."

"pgadmin" "CHANGE_ME_STRONG_ADMIN_PASSWORD"
"app1_user" "CHANGE_ME_APP1_PASSWORD"
"app2_user" "CHANGE_ME_APP2_PASSWORD"
